# prj.conf — comb readout, XIAO nRF52840

CONFIG_PWM=y
CONFIG_ADC=y
CONFIG_GPIO=y

CONFIG_FPU=y            # float IIR in main loop

CONFIG_LOG=y
CONFIG_PRINTK=y

# Disable unused subsystems to keep build lean
CONFIG_BT=n
CONFIG_NETWORKING=n
CONFIG_SHELL=n

